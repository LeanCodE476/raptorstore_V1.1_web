import { Container, Typography } from "@mui/material";

const NavBar = () => {
    return (
        <Container sx={{width:'100%',height:'2rem',bgcolor:'blue',outline:'1px solid red',mt:'3rem'}}>
            <Typography> Desplegar menu</Typography>
        </Container>)
};

export default NavBar;
